[project]
name = "goal-detection"
version = "0.1.0"
description = "HHOF Goalie Simulator - camera-based goal detection"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "opencv-python",
    "websockets",
    # arena_api is distributed by Lucid Vision as a .whl file alongside the
    # Arena SDK - it is not on PyPI. install.ps1 handles this automatically.
]

[tool.uv]
package = false
